public class LoginResponse
{
    public string? Token { get; set; }
    // You can add more properties if needed, such as user information (e.g., username, roles) but ensure they do not expose sensitive information.
}
