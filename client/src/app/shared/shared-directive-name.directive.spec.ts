import { SharedDirectiveNameDirective } from './shared-directive-name.directive';

describe('SharedDirectiveNameDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedDirectiveNameDirective();
    expect(directive).toBeTruthy();
  });
});
