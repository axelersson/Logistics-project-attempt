import { SharedPipeNamePipe } from './shared-pipe-name.pipe';

describe('SharedPipeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SharedPipeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
